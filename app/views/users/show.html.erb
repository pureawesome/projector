<h1><%= @user.first_name %></h1>

<table>
  <thead>
    <tr>
      <td><%= "#{pluralize(@tasks.count, "tasks")}" %></td>
    </tr>
  </thead>
  <tbody>
    <tr>
      <% @tasks.each do |t| %>
      <td>
        <%= t.name %>
      </td>
      <% end %>
    </tr>
  </tbody>
</table>

<p class="btn btn-lg btn-default"><%= link_to('Edit User', edit_user_path(@user)) %></p>

<p class="btn btn-default"><%= link_to('Back to Users', users_path) %></p>
